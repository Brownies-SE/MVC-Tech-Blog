{{! //Something }}
{{#each data as |post|}}

  <div
    class="card border-dark mb-3 col-lg-4 col =lg-offset"
    style="max-width: 50rem;"
  >
    <div class="card-header">{{post.title}}</div>
    <div class="card-body text-dark">
      <p class="card-text">{{post.post_text}}</p>
    </div>
  </div>
{{/each}}